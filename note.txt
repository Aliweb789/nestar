graphQL -> mahsus schema query languagega ega

.env filedan foydalanish uchun
import { ConfigModule } from "@nestjs/config"
  imports: [
    ConfigModule.forRoot()
  ],

graphQL implementation
import { GraphQLModule } from "@nestjs/graphql"
  imports: [
    ConfigModule.forRoot(),GraphQLModule.forRoot({ driver: ApolloDriver, playground: true, uploads: false, autoSchemaFile: true })
  ] --> eng kamida bitta query hosil qilinishi kerak
playground: true, --> page ga borib GraphQl ni practice qilish mumkin


GraphQl--> asosiy --> resolver(query(get), mutation(post))
REst api --> asosiy--> controller


graphQL da enumlarni togridan togri ishlata olmaymiz:
--> import { registerEnumType } from "@nestjs/graphql" qilishim kerak